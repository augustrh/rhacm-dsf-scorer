apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpu-load
  namespace: dynamic-scoring
spec:
  replicas: 4
  selector:
    matchLabels:
      app: cpu-load
  template:
    metadata:
      labels:
        app: cpu-load
    spec:
      containers:
        - name: cpu-load
          image: busybox
          command: ["sh", "-c", "while true; do :; done"]
          resources:
            requests:
              cpu: "500m"
            limits:
              cpu: "1"
